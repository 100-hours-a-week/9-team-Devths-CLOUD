apiVersion: apps/v1
kind: Deployment

metadata:
  name: devths-ai-worker-extract

spec:
  replicas: 1

  template:
    metadata:
      labels:
        environment: prod

    spec:
      containers:
        - name: devths-ai-worker-extract

          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "768Mi"
              cpu: "500m"
