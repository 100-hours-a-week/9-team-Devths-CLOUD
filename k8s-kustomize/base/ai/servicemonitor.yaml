apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ai-servicemonitor
  labels:
    app: ai
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: ai
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
