# SecretStore.yaml
apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: ssm-store
  namespace: dev-app
spec:
  provider:
    aws:
      service: ParameterStore
      region: ap-northeast-2